created_at = str
views_count = str
title = str
duration = str
url = str

video_object = dict[
               created_at: str,
               views_count: str,
               title: str,
               duration: str,
               url: str
               ]

list_of_video_objects = list[video_object]
